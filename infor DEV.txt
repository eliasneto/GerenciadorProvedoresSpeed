
# Criar maquina Virtual:
python3 -m venv .venv

# Rode este comando para instalar o Django e o REST Framework (essencial para sistemas de gest?o):
pip install django djangorestframework python-decouple

# Criar o Projeto Principal
python -m django startproject config .

# Criar a pasta para os M?dulos
mkdir apps

# Criar o M?dulo Core
python manage.py startapp core apps/core

# 1. Cria os arquivos de migraÃ§Ã£o para o mÃ³dulo core
python manage.py makemigrations core

# 2. Aplica as mudan?as no banco de dados
python manage.py migrate

#Clonar um repositorio:
git clone -b dev https://github.com/eliasneto/GerenciadorProvedoresSpeed.git

#Criar arquivo requirements:
pip freeze > requirements.txt

#Instalar apartir do arquivo requirements:
pip install -r requirements.txt

# Remover um Container:
docker rm -f ID_OU_NOME

# lista e remove imagens:
docker images
docker rmi ID_DA_IMAGEM

#Lista e exclui Volumes:
docker volume ls  # Para ver o nome do volume
docker volume rm NOME_DO_VOLUME

# EXCLUIR Container
docker rm NOME_OU_ID_DO_CONTAINER

# Comando para excluir pasta com arquivos dentro no linux: 
rm -rf nome_da_pasta

# Comando para criar pasta no linux

# subindo docker limpando o cache: 
docker-compose down
docker-compose build --no-cache
docker-compose up -d
docker-compose up -d --build

# LOGS DO CONTAINER:
docker logs -f speed_sistema
docker logs --tail 50 -f speed_sistema
docker logs -f speed_backup_diario

# Comando no docker:
# Cria os arquivos de migraÃ§Ã£o para todo mundo
docker exec -it speed_sistema python manage.py makemigrations

# Cria as tabelas de verdade no MySQL
docker exec -it speed_sistema python manage.py migrate

# Destruir um volume: docker compose down -v

# Sobre escrever o que tem no git:
git push --force origin main

# ALTERA BRANCH
git checkout homolog

#Altera a branch e copia o main
git checkout -b homolog

#Roda antes de fazer commit para verificar se o codigo est? ok:
.\.venv\Scripts\python.exe scripts\verificar_repositorio.py

# COMPATIBILIDADE COM VOLUME MYSQL LEGADO
# Se um ambiente antigo subir com erro "Access denied for user 'speed_app'",
# testar primeiro as credenciais antigas do volume:
#   Banco: speed_banco
#   Usuario app legado: speed_user
#   Senha app legada: speed_password
#   Root legado: root_super_password
#
# Teste do root legado:
docker compose exec speed_db mysql -uroot -proot_super_password
#
# Promocao do volume legado para o usuario novo sem apagar dados:
#   CREATE USER IF NOT EXISTS 'speed_app'@'%' IDENTIFIED BY 'SpeedApp!2026#Prod';
#   ALTER USER 'speed_app'@'%' IDENTIFIED BY 'SpeedApp!2026#Prod';
#   GRANT ALL PRIVILEGES ON speed_banco.* TO 'speed_app'@'%';
#   FLUSH PRIVILEGES;
#
# Depois ajustar o .env para:
#   DB_NAME=speed_banco
#   DB_USER=speed_app
#   DB_PASSWORD=SpeedApp!2026#Prod

O pacote do Linux: Mesmo o Docker fazendo o trabalho pesado, o "motor" do Linux hospedeiro precisa saber como falar com o Windows. No terminal do seu servidor Linux, vocÃª sÃ³ precisa rodar este comando uma Ãºnica vez na vida para dar essa habilidade ao servidor:
Bash
sudo apt-get update && sudo apt-get install cifs-utils -y


Eu queria uma melhoria no meu sistema, eu queria que uma proposta fosse vinculada a um prospecto e que um prospecto possa ter vÃ¡rias propostas.
Porque isso?. Eu quero ter um botÃ£o dentro do prospecto onde tenha a opÃ§Ã£o de proposta, aÃ­ tenha uma tela com todas as propostas desse cliente.
cada proposta vai ter o status de "Em negociaÃ§Ã£o". "InviÃ¡vel", "Proposta Aceita".

# COmando para rodar o backup:
docker exec -it speed_backup_diario sh -c 'DATA=$(date +%Y-%m-%d_TESTE); mysqldump -h speed_db -u speed_user -pspeed_password speed_banco > /tmp/banco_$DATA.sql; zip -r /mnt/windows/backup_speed_$DATA.zip /app/media /tmp/banco_$DATA.sql > /dev/null; rm /tmp/banco_$DATA.sql; echo "Backup manual enviado para o Windows!"'

# PASSO A PASSO HOLLBACK
Elias, essa Ã© a pergunta de ouro de todo engenheiro de software! Fazer o backup Ã© sÃ³ 50% do trabalho; os outros 50% sÃ£o saber fazer o Restore (o rollback) no momento do desespero, quando o banco corrompe ou alguÃ©m apaga o que nÃ£o devia.

O processo de restauraÃ§Ã£o no Docker Ã© extremamente simples. Como o nosso backup salva tanto o banco de dados (.sql) quanto os anexos (media/) dentro de um .zip, o passo a passo Ã© este:

ð Guia de SobrevivÃªncia: Restaurando o Banco de Dados
Passo 1: Traga o arquivo de volta para o Linux
VÃ¡ na sua mÃ¡quina Windows, pegue o arquivo de backup desejado (ex: backup_speed_2026-03-27_02-00.zip) e copie ele para dentro do seu servidor Linux, na mesma pasta onde estÃ¡ o seu docker-compose.yml (~/SGP_Speed/GerenciadorProvedoresSpeed).
(VocÃª pode usar programas como WinSCP, FileZilla, ou o MobaXterm para arrastar o arquivo para lÃ¡).

Passo 2: Descompacte o cofre
No terminal do Linux, rode o comando para extrair o .zip:

Bash
unzip backup_speed_2026-03-27_02-00.zip
Como nÃ³s configuramos o zip para salvar a estrutura exata, ele vai extrair duas coisas na sua pasta atual:

Uma pasta tmp/ (com o seu arquivo .sql dentro).

Uma pasta app/media/ (com os seus anexos).

Passo 3: A MÃ¡gica do Restore (Injetando no Banco)
Agora vamos pegar aquele arquivo .sql que estÃ¡ dentro da pasta tmp/ e "injetar" ele direto para dentro do container do MySQL que estÃ¡ rodando.

Rode este comando (substituindo o nome do arquivo .sql pelo nome exato que foi extraÃ­do):

Bash
docker exec -i speed_mysql mysql -u root -proot_super_password speed_banco < tmp/banco_2026-03-27_02-00.sql
â ï¸ Detalhe Ninja: Note que usamos a setinha < apontando para a esquerda. Ela diz para o Linux: "Pegue todo o texto desse arquivo SQL e jogue para dentro do comando mysql lÃ¡ no container".

Passo 4: Restaurando os Anexos (Opcional)
Se vocÃª tambÃ©m perdeu os arquivos fÃ­sicos (fotos, PDFs, etc.) e precisa restaurar a pasta media, Ã© sÃ³ copiar os arquivos que foram extraÃ­dos em app/media/ para a sua pasta ./media verdadeira do projeto:

Bash
cp -r app/media/* ./media/
Pós-Crise
Depois que o banco de dados voltar Ã  vida e o sistema estiver rodando liso novamente, vocÃª pode apagar a sujeira que o unzip deixou no seu servidor Linux para nÃ£o gastar espaÃ§o:

Bash
rm -rf tmp/ app/ backup_speed_*.zip
Ã isso! Com esse Ãºnico comando do docker exec -i, o banco de dados lÃª o .sql, destrÃ³i as tabelas corrompidas e recria todas elas exatamente como estavam no momento do backup.

Gostaria de aproveitar que o sistema estÃ¡ todo "azeitado" e simular um teste de restore na prÃ¡tica com aquele .zip de teste que geramos, ou prefere seguir com o desenvolvimento do sistema web?



